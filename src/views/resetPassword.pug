<!DOCTYPE html>
html(lang="en")
  head
    include partials/head
  body.login
    .container-form
      h1 Reestablecer contraseñaa
      

      form#form.box.box-login(method="POST" action="/reset-password")

        //pre= vardump(errors)

        span(style="font-style:italic") Inserta tu email para recuperar la contraseña

        .field
          label(for="email") Email
          input(type="text" name="email" id="email" placeholder="Inserta tu email" value= email)

        if error
          span.alert.error= error

        if errors.length
            span.alert.error= errors[0].msg

        .field
          input.btn(type="submit" value="Reestablecer")
        
        .field.actions
          a(href="/iniciar-sesion") Ya lo recordé
        
